name: FTG Foundation Daily Refresh

# ------------------------------------------------------------
# Schedule
# Every 2 hours from 6am–8pm Pacific
# (cron is UTC)
#
# 6am  PT → 14:00 UTC
# 8am  PT → 16:00 UTC
# 10am PT → 18:00 UTC
# 12pm PT → 20:00 UTC
# 2pm  PT → 22:00 UTC
# 4pm  PT → 00:00 UTC
# 6pm  PT → 02:00 UTC
# 8pm  PT → 04:00 UTC
# ------------------------------------------------------------
on:
  schedule:
    # Weekdays only (Mon–Fri)
    # Every 2 hours from 6am–8pm Pacific
    - cron: "0 14,16,18,20,22 * * 1-5"   # 6am–2pm PT
    - cron: "0 0,2,4 * * 2-6"            # 4pm–8pm PT (same weekdays, UTC rollover)
  workflow_dispatch:


# ------------------------------------------------------------
# Concurrency
# Prevent overlapping Foundation refreshes
# ------------------------------------------------------------
concurrency:
  group: foundation-refresh
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest

    env:
      FOUNDATION_SQL_USER: ${{ secrets.FOUNDATION_SQL_USER }}
      FOUNDATION_SQL_PASSWORD: ${{ secrets.FOUNDATION_SQL_PASSWORD }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Microsoft SQL ODBC Driver
        run: |
          sudo apt-get update
          sudo apt-get install -y curl gnupg apt-transport-https
          curl https://packages.microsoft.com/keys/microsoft.asc | sudo gpg --dearmor -o /usr/share/keyrings/microsoft.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/microsoft.gpg] https://packages.microsoft.com/ubuntu/22.04/prod jammy main" | sudo tee /etc/apt/sources.list.d/mssql-release.list
          sudo apt-get update
          sudo ACCEPT_EULA=Y apt-get install -y msodbcsql17 unixodbc-dev

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python packages
        run: |
          pip install --upgrade pip
          pip install pyodbc pandas

      - name: Run full FTG pipeline
        run: python scripts/run_all.py

      - name: Commit and push outputs
        run: |
          git config user.name "FTG Data Bot"
          git config user.email "bot@ftg.local"
          git add data public/data
          git commit -m "Automated FTG data refresh" || echo "No changes to commit"
          git push
